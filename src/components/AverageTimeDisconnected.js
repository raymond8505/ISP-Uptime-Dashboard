import React from "react";
import Highcharts from 'highcharts';
import HighchartsReact from 'highcharts-react-official'

const AverageTimeDisconnected = ({log}) => {

    return (<div className="AverageTimeDisconnected">
        
    </div>);
}

export default AverageTimeDisconnected;